# Tidepool

A small kanban board. No dependencies, no build step.

```bash
node server.js
# http://localhost:3000
```

## Layout

- `server.js` — the whole back end
- `public/` — the whole front end
- `seed.json` — the board you start with
